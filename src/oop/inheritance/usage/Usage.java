package oop.inheritance.usage;

public class Usage {

    /*
     * Inheritance - IS A relationship
     *
     * - when your derived class truly if the type
     * you are extending, not merly contain the same
     * data, the subclass gains the methods and the data
     * - when makes sense the derived class to have all
     * the methods of the superclass
     *
     * ChildClass extends ParentClass
     *
    */
    public static void main(String[] args) {

    }
}
